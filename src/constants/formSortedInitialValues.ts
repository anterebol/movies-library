import { KeyAsString } from "@/types/KeyAsString";

const initialValues = {
  genre: '',
  release_year: '',
  rating_from: '',
  rating_to: '', 
  sort_by: 'popularity.desc'
} as KeyAsString;

export default initialValues;